# testpoc

Harmless Maven parent used to reproduce an Anypoint Studio security-report behavior.

The demonstration writes a local marker file and opens Windows Calculator when the reviewer explicitly runs the test Mule application. Importing the project alone does not execute it.
